{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "curl -fsSL https://flutter.dev/setup.sh | bash && export PATH=\"$PATH:$HOME/flutter/bin\" && flutter --version && flutter pub get",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}